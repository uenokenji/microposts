<% if @feed_items.any? %>
  <%= page_entries_info @feed_items, entry_name: "micropost" %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= paginate @feed_items, :params => {:controller => 'static_pages', :action => 'home'} %>
<% end %>